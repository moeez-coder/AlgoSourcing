# Template only — copy to .env locally if you ever need a local process to read
# these as env vars. Never put real values in a file tracked by git (.env is
# gitignored). For the actual, correct place these should live so every
# session/agent can use them, see sourcing/TOOLS.md → "Where API keys live".

BLITZ_API_KEY=
# COLDIQ_API_KEY=   # uncomment once we know Cold IQ's actual env var name
